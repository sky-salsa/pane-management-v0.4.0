---
phase: 03-dashboard-canvas
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - workspace-resume/src/components/layout/QuickLaunch.tsx
  - workspace-resume/src/App.tsx
  - workspace-resume/src/index.css
  - workspace-resume/src/components/DevPanel.tsx
autonomous: false
requirements: [DASH-01, DASH-02, DASH-07, PERF-03]

must_haves:
  truths:
    - "Quick-launch strip shows pinned projects as compact buttons for fast access"
    - "Clicking a quick-launch button assigns the project to the next available pane and launches it"
    - "App pins to Windows taskbar (standard Tauri window behavior)"
    - "Dashboard is visually polished with modern dark theme and accent color"
    - "Dashboard remains responsive with 20+ project cards in the sidebar"
    - "DevPanel.tsx is removed (no longer imported)"
    - "All pane assignments and project metadata persist across app restarts"
    - "Human verifies the full flow: sidebar, drag-to-pane, resume, presets, tier management"
  artifacts:
    - path: "workspace-resume/src/components/layout/QuickLaunch.tsx"
      provides: "Pinned project quick-launch strip below pane grid"
      contains: "export function QuickLaunch"
  key_links:
    - from: "workspace-resume/src/components/layout/QuickLaunch.tsx"
      to: "workspace-resume/src/contexts/AppContext.tsx"
      via: "useApp for pinned projects"
      pattern: "projectsByTier.*pinned"
    - from: "workspace-resume/src/components/layout/QuickLaunch.tsx"
      to: "workspace-resume/src/lib/tauri-commands.ts"
      via: "sendToPane for quick launch"
      pattern: "sendToPane"
    - from: "workspace-resume/src/components/layout/QuickLaunch.tsx"
      to: "workspace-resume/src/lib/path.ts"
      via: "import toWslPath"
      pattern: "import.*toWslPath.*from.*path"
---

<objective>
Complete Phase 3 integration: build the QuickLaunch strip, polish the visual theme, remove the old DevPanel, and verify the full end-to-end flow with a human checkpoint.

Purpose: This plan finishes all remaining features (quick-launch for pinned projects, visual polish), cleans up legacy code, and gets human verification that the complete tmux pane manager works as designed.

Output: QuickLaunch.tsx component, polished CSS, DevPanel removed, human-verified complete flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-canvas/03-CONTEXT.md
@.planning/phases/03-dashboard-canvas/03-RESEARCH.md
@.planning/phases/03-dashboard-canvas/03-04-SUMMARY.md

<interfaces>
From workspace-resume/src/contexts/AppContext.tsx:
```typescript
export function useApp(): {
  state: {
    projects: ProjectWithMeta[];
    selectedTmuxSession: string | null;
    selectedTmuxWindow: number | null;
    tmuxPanes: TmuxPane[];
    paneAssignments: Record<string, string>;
  };
  projectsByTier: (tier: ProjectTier) => ProjectWithMeta[];
  refreshTmuxState: () => void;
  refreshProjects: () => void;
};
```

From workspace-resume/src/lib/tauri-commands.ts:
```typescript
export async function sendToPane(sessionName: string, windowIndex: number, paneIndex: number, command: string): Promise<void>;
export async function setPaneAssignment(paneIndex: number, encodedProject: string | null): Promise<Record<string, string>>;
export async function createPane(sessionName: string, windowIndex: number, direction: string): Promise<TmuxPane[]>;
```

From workspace-resume/src/lib/path.ts (created in Plan 04):
```typescript
export function toWslPath(winPath: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build QuickLaunch strip, polish theme, and clean up DevPanel</name>
  <read_first>
    - workspace-resume/src/App.tsx
    - workspace-resume/src/contexts/AppContext.tsx
    - workspace-resume/src/components/DevPanel.tsx
    - workspace-resume/src/index.css
    - workspace-resume/src/lib/tauri-commands.ts
    - workspace-resume/src/lib/path.ts
  </read_first>
  <files>
    workspace-resume/src/components/layout/QuickLaunch.tsx,
    workspace-resume/src/App.tsx,
    workspace-resume/src/index.css,
    workspace-resume/src/components/DevPanel.tsx
  </files>
  <action>
    **Create QuickLaunch.tsx** at `workspace-resume/src/components/layout/QuickLaunch.tsx`:
    - Import `useApp` from contexts/AppContext
    - Import `For`, `Show` from solid-js
    - Import `sendToPane`, `setPaneAssignment`, `createPane` from tauri-commands
    - Import `toWslPath` from `lib/path` (already created by Plan 04 -- do NOT recreate or inline)
    - Get pinned projects: `const pinned = () => useApp().projectsByTier("pinned")`
    - Render a horizontal strip of compact buttons, one per pinned project.
    - Each button shows the project display name (or derived name) in a pill/chip style.
    - **Click behavior** (quick-launch to next available pane):
      1. Check `state.tmuxPanes` and `state.paneAssignments` to find the first unassigned pane (a pane whose index does not appear as a key in paneAssignments, or whose assignment is null).
      2. If no unassigned pane exists, create a new one via `createPane(session, window, "h")` and use the newly created pane.
      3. Assign the project to that pane via `setPaneAssignment(paneIndex, encodedProject)`.
      4. Build the command: determine WSL path from project.actual_path via `toWslPath(project.actual_path)`, build `cd "${wslPath}" && claude -r ${boundSession || ""}`.
      5. Send command via `sendToPane(session, window, paneIndex, command)`.
      6. Refresh tmux state and projects.
    - If no projects are pinned, show subtle message: "Pin projects to see them here" or simply don't render the strip at all (just the empty container).
    - Active indicator: If a pinned project is currently assigned to any pane, show a small green dot on its quick-launch button.
    - Styling: horizontal scroll if too many pins, compact pills with bg `#2d2d5a`, accent border on hover, rounded (20px for pill shape), 28px height, small text (0.8rem).

    **Update App.tsx:**
    - Import `QuickLaunch` from components/layout/QuickLaunch
    - Replace the quick-launch placeholder comment/div with `<QuickLaunch />`

    **Delete DevPanel.tsx**: Remove `workspace-resume/src/components/DevPanel.tsx`. It is no longer imported anywhere. If there's any utility function in DevPanel.tsx that's still useful (like `formatBytes`, `truncate`, `projectName`), move it to an appropriate lib file before deleting. Check that no other file imports from DevPanel.

    **CSS polish pass** on index.css:
    - Remove all `.dev-panel*` CSS rules (the entire "Dev Panel" section)
    - Ensure consistent spacing, border colors, and accent color usage across all components
    - QuickLaunch styles:
      ```css
      .quick-launch { border-top: 1px solid #2d2d4a; padding: 8px 12px; display: flex; gap: 8px; align-items: center; overflow-x: auto; min-height: 48px; }
      .quick-launch-btn { background: #2d2d5a; color: #c8c8e0; border: 1px solid #3d3d6a; border-radius: 20px; padding: 4px 14px; cursor: pointer; font-size: 0.8rem; white-space: nowrap; transition: all 0.15s ease; display: flex; align-items: center; gap: 6px; }
      .quick-launch-btn:hover { border-color: #6366f1; background: #3d3d6a; }
      .quick-launch-empty { color: #555; font-size: 0.78rem; font-style: italic; }
      ```
    - Verify all existing reused styles (badge, toast, resume-btn, session-card, settings-panel) still work correctly with the new layout
    - Ensure the font family, background color, and text color from `:root` are consistent
    - Add a subtle transition on sidebar section collapse/expand
    - Verify scroll behavior: sidebar scrolls independently, main area scrolls independently, quick-launch scrolls horizontally
  </action>
  <verify>
    <automated>cd workspace-resume && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "export function QuickLaunch"
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "projectsByTier"
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "pinned"
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "sendToPane"
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "toWslPath"
    - File workspace-resume/src/components/layout/QuickLaunch.tsx contains "from.*lib/path" or "from \"../../lib/path\""
    - File workspace-resume/src/App.tsx contains "QuickLaunch"
    - File workspace-resume/src/App.tsx does NOT contain "DevPanel"
    - File workspace-resume/src/index.css contains ".quick-launch"
    - File workspace-resume/src/index.css does NOT contain ".dev-panel {"
    - `npx tsc --noEmit` passes with 0 errors
  </acceptance_criteria>
  <done>QuickLaunch strip renders pinned projects as pill buttons with quick-launch-to-pane behavior using shared toWslPath from lib/path.ts. DevPanel removed. CSS polished with consistent dark theme and accent color. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Integration build and smoke test</name>
  <read_first>
    - workspace-resume/src/App.tsx
    - workspace-resume/src-tauri/src/lib.rs
    - workspace-resume/package.json
  </read_first>
  <files>workspace-resume/src/App.tsx</files>
  <action>
    Run a complete integration build to verify everything compiles and links:

    1. **TypeScript check**: `cd workspace-resume && npx tsc --noEmit` -- should have 0 errors
    2. **Vite build**: `cd workspace-resume && npm run build` -- should succeed, check bundle size is reasonable (< 500KB JS)
    3. **Cargo check**: `cd workspace-resume/src-tauri && cargo check` -- should compile all Rust code including new commands
    4. **Cargo test**: `cd workspace-resume/src-tauri && cargo test --lib` -- all Rust tests pass
    5. **Dev launch**: `cd workspace-resume && npm run tauri dev` -- app launches, verify:
       - Window appears with the four-region layout
       - TopBar shows session/window tabs (or empty state)
       - Sidebar shows discovered projects grouped by tier
       - Main area shows pane grid (or empty state)
       - Quick-launch strip renders at bottom

    If any build errors occur, fix them:
    - Common issues: missing imports, type mismatches between Rust and TS, CSS class name typos
    - If solid-dnd has TypeScript issues with directive usage, ensure the `solid-dnd.d.ts` file is being picked up by tsconfig

    Fix any issues found. This task is about ensuring the entire system compiles and renders without errors.
  </action>
  <verify>
    <automated>cd workspace-resume && npx tsc --noEmit && npm run build && cd src-tauri && cargo test --lib 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
    - `cargo test --lib` shows 0 failures
    - No import of DevPanel exists in any file
  </acceptance_criteria>
  <done>Full integration build passes: TypeScript, Vite, Cargo all succeed. All Rust tests pass. App launches and renders the new layout without console errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 3 dashboard</name>
  <files>none</files>
  <action>
    Present the human-verify checkpoint for manual testing of the complete Phase 3 tmux pane manager dashboard.
  </action>
  <what-built>
    Complete Phase 3 tmux pane manager dashboard:
    - Four-region layout: TopBar (tmux tabs), Sidebar (tiered project browser), MainArea (pane grid), QuickLaunch (pinned shortcuts)
    - Drag projects from sidebar to pane slots to assign and launch
    - Layout presets (2-pane, 4-pane, main+stack, etc.)
    - Project tier management (pin/unpin/pause/archive)
    - Inline project rename
    - Session binding (use specific session vs most recent)
    - Quick-launch strip for pinned projects
    - Relative timestamps on sessions
    - Modern dark theme with indigo accent
  </what-built>
  <how-to-verify>
    Start the app: `cd workspace-resume && npm run tauri dev`

    **1. Layout verification:**
    - Confirm four regions visible: top bar, left sidebar, main area, bottom quick-launch strip
    - Sidebar should show discovered projects grouped under "Active" section (all default to Active tier)

    **2. tmux hierarchy navigation:**
    - If tmux is running in WSL, session tabs should appear in top bar
    - Click a session tab, then a window tab -- main area should update

    **3. Project management:**
    - Change a project's tier to "Pinned" via the tier dropdown on its card
    - Confirm it moves to the Pinned section at top of sidebar AND appears in quick-launch strip
    - Double-click a project name to rename it inline. Type a new name, press Enter. Confirm name updates.
    - Change a project to "Paused" -- confirm it moves to collapsible Paused section

    **4. Session browsing:**
    - Click "Sessions" on a project card to expand the session list
    - Confirm sessions show relative timestamps ("2h ago", "3d ago") and duration
    - Click "Use this session" to bind a specific session to the project

    **5. Pane management:**
    - Select a tmux session and window
    - Use "Split H" or "Split V" to create panes
    - Select a preset layout (e.g., "4 Grid")
    - Drag a project from the sidebar into a pane slot
    - Confirm: green border highlights the drop zone during drag, and after drop the pane shows the project name
    - Confirm: tmux actually received the command (check in your terminal that claude launched in the correct pane)

    **6. Quick-launch:**
    - Click a pinned project in the quick-launch strip
    - Confirm it auto-assigns to the next available pane and launches

    **7. Resume buttons (pane-first launch):**
    - Click Resume on a project card in the sidebar
    - Confirm it auto-assigns to the next available pane (same as quick-launch) and launches -- NOT a free-floating terminal window
    - Expand sessions, click Resume on a specific session
    - Confirm it also goes through pane assignment, not free-floating launch

    **8. Persistence:**
    - Close and reopen the app
    - Confirm project tiers, display names, and pane assignments are preserved

    **9. Performance:**
    - If you have 20+ projects, confirm sidebar scrolls smoothly with no jank
  </how-to-verify>
  <verify>Human confirms all 9 verification areas pass</verify>
  <done>Human approves the complete Phase 3 dashboard flow.</done>
  <resume-signal>Type "approved" to complete Phase 3, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. Full build: `npx tsc --noEmit && npm run build && cargo test --lib` -- all pass
2. App launches and renders four-region layout
3. Human verifies: drag-to-pane, presets, tier management, rename, quick-launch, pane-first resume, persistence
</verification>

<success_criteria>
- QuickLaunch strip shows pinned projects with one-click launch-to-pane
- QuickLaunch imports toWslPath from lib/path.ts (shared with App.tsx, not duplicated)
- DevPanel.tsx removed, all CSS dev-panel rules removed
- Dark theme consistent across all components with indigo accent
- Full integration build passes (TS + Vite + Cargo)
- Human verifies complete Phase 3 flow end-to-end including pane-first resume buttons
- All pane assignments and project metadata persist across restarts
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-canvas/03-05-SUMMARY.md`
</output>
