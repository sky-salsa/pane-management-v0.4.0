---
phase: 02-session-resume
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - workspace-resume/src/components/SettingsPanel.tsx
  - workspace-resume/src/components/DevPanel.tsx
  - workspace-resume/src/index.css
  - workspace-resume/src-tauri/capabilities/default.json
autonomous: false
requirements:
  - RESU-01
  - RESU-02
  - RESU-03
  - RESU-04
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "User can click Resume on a project and a Warp terminal opens at the project directory"
    - "User can pick a specific session and resume it via claude -r <session_id>"
    - "User can switch terminal backend in settings panel"
    - "Failed launches show an error toast and log to persistent error log"
    - "Active sessions show a green indicator"
  artifacts:
    - path: "workspace-resume/src/components/SettingsPanel.tsx"
      provides: "Terminal settings dropdown + error log viewer"
      contains: "TerminalBackend"
    - path: "workspace-resume/src-tauri/capabilities/default.json"
      provides: "Tauri permissions for shell operations"
      contains: "shell"
  key_links:
    - from: "workspace-resume/src/components/DevPanel.tsx"
      to: "workspace-resume/src/lib/tauri-commands.ts"
      via: "resumeSession() call in handleResume"
      pattern: "resumeSession"
    - from: "workspace-resume/src/components/SettingsPanel.tsx"
      to: "workspace-resume/src/lib/tauri-commands.ts"
      via: "updateTerminalSettings() and getErrorLog()"
      pattern: "updateTerminalSettings"
    - from: "workspace-resume/src-tauri/capabilities/default.json"
      to: "Tauri security layer"
      via: "permission grants for shell:default and store:default"
      pattern: "shell"
---

<objective>
Wire the frontend resume UI to the backend launcher, add settings panel for terminal configuration, update Tauri permissions, and verify the full end-to-end flow with a human checkpoint.

Purpose: This is the integration plan that connects Plan 01 (backend) and Plan 02 (frontend) into a working resume flow. Includes the settings panel (per RESU-04 configurable backend) and the manual verification checkpoint.
Output: Fully functional session resume -- click Resume, terminal opens, Claude session resumes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-resume/02-CONTEXT.md
@.planning/phases/02-session-resume/02-RESEARCH.md
@.planning/phases/02-session-resume/02-01-SUMMARY.md
@.planning/phases/02-session-resume/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (backend) -->
IPC commands available:
- resume_session(encoded_project, session_id, project_path) -> ResumeResult
- get_active_sessions() -> Vec<ActiveSession>
- get_terminal_settings() -> TerminalSettings
- update_terminal_settings(backend) -> TerminalSettings
- get_error_log() -> Vec<ErrorLogEntry>
- clear_error_log() -> void

<!-- From Plan 02 (frontend) -->
TypeScript wrappers in tauri-commands.ts:
- resumeSession(encodedProject, sessionId, projectPath): Promise<ResumeResult>
- getActiveSessions(): Promise<ActiveSession[]>
- getTerminalSettings(): Promise<TerminalSettings>
- updateTerminalSettings(backend): Promise<TerminalSettings>
- getErrorLog(): Promise<ErrorLogEntry[]>
- clearErrorLog(): Promise<void>

Types in types.ts:
- TerminalBackend = "warp" | "powershell"
- TerminalSettings { backend: TerminalBackend }
- ResumeResult { pid, terminal, session_id }
- ActiveSession { session_id, pid, terminal, is_alive }
- ErrorLogEntry { timestamp, terminal, error, project_path }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings panel and update Tauri permissions</name>
  <read_first>
    - workspace-resume/src/components/DevPanel.tsx
    - workspace-resume/src/lib/tauri-commands.ts
    - workspace-resume/src/lib/types.ts
    - workspace-resume/src-tauri/capabilities/default.json
    - workspace-resume/src/index.css
  </read_first>
  <files>
    workspace-resume/src/components/SettingsPanel.tsx,
    workspace-resume/src/components/DevPanel.tsx,
    workspace-resume/src/index.css,
    workspace-resume/src-tauri/capabilities/default.json
  </files>
  <action>
    **1. Create `workspace-resume/src/components/SettingsPanel.tsx`:**

    A simple settings component (per user decision: "Simple settings UI -- expose a dropdown"):
    ```tsx
    import { createSignal, createResource, For, Show, onMount } from "solid-js";
    import { getTerminalSettings, updateTerminalSettings, getErrorLog, clearErrorLog } from "../lib/tauri-commands";
    import type { TerminalSettings, ErrorLogEntry } from "../lib/types";
    ```

    Component structure:
    - **Terminal Backend dropdown**: `<select>` with options "Warp" (value="warp") and "PowerShell" (value="powershell"). On change, call `updateTerminalSettings(value)`.
    - Load current setting on mount via `getTerminalSettings()`.
    - **Error Log section**: collapsible `<details>` element
      - Load errors via `getErrorLog()` on mount
      - Display each error: timestamp, terminal, error message, project path
      - "Clear Log" button calls `clearErrorLog()` and refreshes
      - Show "No errors" when log is empty

    **2. Update `workspace-resume/src/components/DevPanel.tsx`:**
    - Import `SettingsPanel` component
    - Add a `[showSettings, setShowSettings]` signal (boolean, default false)
    - Add a "Settings" button in the header area (next to the title)
    - When clicked, toggle `showSettings` signal
    - Render `<Show when={showSettings()}><SettingsPanel /></Show>` between the header and the project table

    **3. Update `workspace-resume/src/index.css`:**
    Add styles for:
    - `.settings-panel` -- bordered container, padding, margin-bottom, background slightly different from main
    - `.settings-panel select` -- styled dropdown
    - `.settings-panel details` -- collapsible error log
    - `.settings-panel .error-entry` -- error log item styling
    - `.settings-btn` -- settings toggle button styling

    **4. Update `workspace-resume/src-tauri/capabilities/default.json`:**
    Read the current file first. Add permissions needed for Phase 2:
    - The store plugin permissions should already be present from Phase 1 (`"store:default"`)
    - Add `"shell:default"` if not present (needed for `tauri-plugin-shell` if used)
    - Ensure the existing permissions are preserved

    Note: The `open` crate and `std::process::Command` do NOT require Tauri permissions -- they bypass the webview security layer since they run in Rust directly. The permissions are only needed if frontend JS calls shell APIs directly (which we don't).
  </action>
  <verify>
    <automated>cd workspace-resume && npx tsc --noEmit 2>&1 | tail -10 && echo "--- Settings check ---" && grep -c "TerminalSettings\|updateTerminalSettings\|getErrorLog" src/components/SettingsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - `grep "SettingsPanel" workspace-resume/src/components/SettingsPanel.tsx` finds the component
    - `grep "updateTerminalSettings" workspace-resume/src/components/SettingsPanel.tsx` finds the settings update call
    - `grep "getErrorLog" workspace-resume/src/components/SettingsPanel.tsx` finds the error log fetch
    - `grep "clearErrorLog" workspace-resume/src/components/SettingsPanel.tsx` finds the clear function
    - `grep "SettingsPanel" workspace-resume/src/components/DevPanel.tsx` finds the import and render
    - `grep "settings-panel" workspace-resume/src/index.css` finds the styles
    - `grep "store:default" workspace-resume/src-tauri/capabilities/default.json` confirms store permissions
  </acceptance_criteria>
  <done>
    Settings panel created with terminal backend dropdown and error log viewer. DevPanel includes settings toggle. Tauri permissions updated. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration build and smoke test</name>
  <read_first>
    - workspace-resume/src-tauri/src/lib.rs
    - workspace-resume/src-tauri/Cargo.toml
    - workspace-resume/src/components/DevPanel.tsx
  </read_first>
  <files>
    workspace-resume/src-tauri/src/lib.rs
  </files>
  <action>
    Run the full integration build to verify everything compiles together:

    1. Run `cd workspace-resume/src-tauri && cargo test` -- all Rust tests pass
    2. Run `cd workspace-resume && npx tsc --noEmit` -- all TypeScript types check
    3. Run `cd workspace-resume/src-tauri && cargo build` -- full Rust build succeeds
    4. Run `cd workspace-resume && npm run build` -- full frontend build succeeds (Vite)

    If any step fails, diagnose and fix:
    - Missing imports: add them
    - Type mismatches between Rust serialization and TypeScript types: align the TS types to match Rust's serde output (snake_case field names)
    - Permission errors in capabilities: add missing permission strings
    - Dependency resolution issues: check Cargo.toml version constraints

    **Do NOT fix issues that require architectural changes** -- flag those for user decision.
  </action>
  <verify>
    <automated>cd workspace-resume/src-tauri && cargo test 2>&1 | tail -5 && cargo build 2>&1 | tail -5 && cd .. && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` output contains "test result: ok"
    - `cargo build` output contains "Finished" (no errors)
    - `npx tsc --noEmit` produces no output (clean)
    - No warnings on new Phase 2 code (existing Phase 1 warnings acceptable)
  </acceptance_criteria>
  <done>
    Full integration build passes: Rust tests green, Rust build clean, TypeScript type-check clean, Vite build succeeds. All Phase 2 backend and frontend code compiles together.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of terminal launch</name>
  <files>
    workspace-resume/src/components/DevPanel.tsx
  </files>
  <action>
    Present the user with the following verification checklist. Start the app with `cd workspace-resume && npm run tauri dev` and pause for human verification.

    **What was built:**
    Complete session resume flow: Rust terminal abstraction (Warp + PowerShell), IPC commands, DevPanel with Resume buttons, session side panel, settings panel, active session tracking, error toasts.

    **Verification steps:**
    1. Run `cd workspace-resume && npm run tauri dev` to start the app in dev mode
    2. **Project list**: Verify projects appear in the table with Resume and Select Session buttons
    3. **Resume (most recent)**: Click Resume on a project that has sessions and where path_exists is OK
       - Expected: A Warp terminal window opens at the project directory
       - If Warp fails: A PowerShell window should open as fallback
       - The terminal should show the Claude session being resumed
       - A success toast should appear briefly
    4. **Select Session**: Click Select Session on a project
       - Expected: Side panel slides in from the right showing session cards
       - Each card shows: session ID (8 chars), duration, last active time, last message preview
       - Click Resume on a specific session card
       - Expected: Terminal opens with `claude -r <session_id>` at the project directory
    5. **Active indicator**: After launching a session, check for green dot on that session/project
    6. **Settings**: Click Settings button, change terminal to PowerShell, resume a session
       - Expected: PowerShell window opens instead of Warp
    7. **Error handling**: Change terminal to PowerShell, temporarily rename powershell.exe (or set to invalid path if possible)
       - Expected: Error toast appears, error logged in Settings > Error Log

    **Known limitations to accept (not bugs):**
    - Warp launch config approach (`warp://launch/`) may not work on Windows -- if so, Warp opens at correct directory but user must type `claude -r` manually. This is a known Warp limitation (research Pitfall 1).
    - PID tracking for Warp is best-effort (research Pitfall 4) -- active indicator may not work for Warp-launched sessions.

    **Resume signal:** Type "approved" if resume flow works, or describe any issues.
  </action>
  <verify>
    <automated>cd workspace-resume/src-tauri && cargo test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Resume button opens terminal at correct project directory
    - User confirms session selection opens specific session via claude -r
    - User confirms Settings panel allows terminal backend switching
    - User confirms error toast appears on failed launch
  </acceptance_criteria>
  <done>
    User has verified the end-to-end resume flow: terminal opens at correct directory, sessions resume via claude -r, settings work, errors display properly. Phase 2 is functionally complete.
  </done>
</task>

</tasks>

<verification>
1. `cd workspace-resume/src-tauri && cargo test` -- all tests pass
2. `cd workspace-resume/src-tauri && cargo build` -- clean build
3. `cd workspace-resume && npx tsc --noEmit` -- zero type errors
4. Manual: Resume button opens terminal at correct directory
5. Manual: Session selection opens specific session via claude -r
6. Manual: Settings panel allows terminal backend switching
</verification>

<success_criteria>
- User can click Resume and a terminal opens at the project directory with claude -r
- User can select a specific session and resume it
- Terminal backend is switchable between Warp and PowerShell via settings
- Error handling works: toasts appear, errors logged
- Active session indicator shows running sessions
- Full build (Rust + TypeScript) passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-resume/02-03-SUMMARY.md`
</output>
